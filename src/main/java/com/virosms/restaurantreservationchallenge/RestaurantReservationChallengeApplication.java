package com.virosms.restaurantreservationchallenge;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * RestaurantReservationChallengeApplication is the main class for the Spring Boot application.
 * It serves as the entry point for the application, initializing the Spring context and starting the application.
 */
@SpringBootApplication
public class RestaurantReservationChallengeApplication {

    /**
     * The main method is the entry point of the application.
     * It starts the Spring Boot application.
     *
     * @param args command line arguments
     */
    public static void main(String[] args) {
        SpringApplication.run(RestaurantReservationChallengeApplication.class, args);
    }

}
