spring.application.name=restaurant-reservation-challenge

# Activa el perfil deseado (dev para H2, prod para PostgreSQL)
spring.profiles.active=dev

# Configuraci�n com�n a todos los perfiles
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Timezone y configuraci�n de JPA global
spring.jpa.open-in-view=false
#spring.jpa.properties.hibernate.jdbc.time_zone=UTC

# Puerto opcional (si quieres cambiarlo del 8080)
# server.port=8085


api.security.token.secret="{JWT_SECRET:my-secret-key}"